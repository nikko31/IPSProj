# ;
# ;
# ;
# ;
# CALCOLA I CODICI PAGHE GIORNALIERI;
# CALCOLA IL SALDO ORE GIORNALIERO;
# CALCOLA IL SALDO ORE MENSILE;
# ESEGUE LA FORMULA SOLTANTO PER APPRENDISTI E DIPENDENTI;
# INIZIALIZZA I CODICI PAGHE GIORNALIERI;
Fai SAL = SAL + ValPa(_SAL);
Fai _SAL = ValPa(_EFF), _SAL = _SAL + ValPa(_GIU), _SAL = _SAL - ValPa(_TEO);
Fai _SCO = 0, _STR = 0;
Se AggOr(01) <> APP E AggOr(01) <> DIP Fai Interrompi;
Se ValPa(_SAL) < 0 Fai _SCO = ValPa(_SAL);
Se ValPa(_SAL) > 0 Fai _STR = ValPa(_SAL);
